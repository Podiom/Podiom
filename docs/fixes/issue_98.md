# Resolution for issue #98: `PODIOM_ADDR` is honoured by the CLI but ignored by `podiomd`, so setting it makes every command report "podiomd is not running" while it is

Automated analysis and solution payload conforming to repository standards.

Closes #98
